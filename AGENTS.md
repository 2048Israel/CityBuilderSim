# For any AI agent

Read `CLAUDE.md`. It is written for every assistant, not only Claude: where
the indexes are (`docs/map`, `docs/dials.md`, `docs/month-order.md`,
`docs/harnesses.md`), the standing rules, and how a batch is done and
verified here.
